# Fixture project: German output language

A minimal project used by tests/localized-prose.test.sh and by
docs/checklists/language-live-check.md. It exists so a run can be pointed at a
project whose effective language is German while the runner itself sits in this
repo, which is English. Nothing here is shipped.

## Conventions
output_language: de
