---
description: Map the business model, how the product creates, delivers, and captures value, on one page, to pressure-test viability. Use early in strategy for a new product/venture. Based on the Business Model Canvas and Lean Canvas; covers the viability risk.
disable-model-invocation: true
---

# Business model

A one-page view of how the whole thing works as a **business**, not just a product.
Pressure-tests the **viability** risk (one of the four product risks). Based on the
Business Model Canvas and the Lean Canvas (the startup-focused variant).

## When to use
- Early strategy for a new product/venture; or whenever "is this actually a business?" is unanswered.

## Process (Lean Canvas, best for new/uncertain)
Fill briefly and honestly:
1. **Problem** (top 1-3) + existing alternatives
2. **Customer segments** (+ early adopters), link `/market-segments`
3. **Unique value proposition**, link `/value-proposition`
4. **Solution** (top features)
5. **Channels**, link `/gtm-plan`
6. **Revenue streams**, link `/pricing`
7. **Cost structure**
8. **Key metrics**, link `/metrics`
9. **Unfair advantage** (hard to copy)

For an established business, use the full **Business Model Canvas** (adds key partners, activities, resources, customer relationships).

## Output
A one-page canvas saved to `docs/`. The riskiest box is an assumption, feed it to `/experiments`.

## Example (freelancer tool, Lean Canvas, abbreviated)
```
Problem: invoicing slow/forgotten | Segment: solo freelancers
UVP: get paid for all your time, automatically | Solution: time -> 1-click invoice
Revenue: $12/mo | Channels: freelancer communities | Key metric: % of time invoiced
Unfair advantage: tracked-time-to-invoice in one step
```

## Quality rules
- One page, honest. A blank box is a **finding** (a hole in the model), not a failure to hide.
- The riskiest box is an assumption to test (`/experiments`), not a fact to assert.
- Tie boxes to the relevant skills (`/pricing`, `/gtm-plan`, `/metrics`); don't silo them.
